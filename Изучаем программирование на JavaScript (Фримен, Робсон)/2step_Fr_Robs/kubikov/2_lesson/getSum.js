'use strict';
/**
 * функция вычисляет сумму параметров
 * @param {number} a 
 * @param {number} b 
 * @returns {number} 
 */

function sum(a, b) {
    return a + b;
    // этот код никогда не выполнится
    alert("Hello world!");
}

let myArr = [5, 6, 3, 8];
let mySum = sum(4, 1);
console.log(new Date().getDay());
console.log(myArr);